# =============================================================================
# Python packages for the FastAPI AI service (Google Gemini)
# =============================================================================
# Install with (from the ai-service/ folder, after creating a virtualenv):
#   pip install -r requirements.txt
# =============================================================================

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-dotenv>=1.0,<2.0
google-genai>=1.0,<2.0
pydantic>=2.0,<3.0
httpx>=0.27,<1.0

# Dev / build checks (no screenshot or video recording tools)
pytest>=8.0,<9.0
